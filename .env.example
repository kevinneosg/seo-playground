# Application database connection string.
# Defaults (in docker-compose) to the local db service if unset.
DATABASE_URL=postgres://postgres:postgres@db:5432/seo

# Postgres service credentials (used by the db service in docker-compose).
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=seo
